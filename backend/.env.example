# MongoDB
MONGO_URI=mongodb://localhost:27017/rightclicked

# JWT
JWT_SECRET=your-jwt-secret-here

# Frontend URL (for CORS — set to your Vercel frontend URL in production)
FRONTEND_URL=http://localhost:5173

# Google reCAPTCHA v3 (optional — leave empty to skip verification in dev)
RECAPTCHA_SECRET=

# ── Ollama AI Configuration ──────────────────────────────────
# Option A: Local Ollama (default — no API key needed)
#   OLLAMA_BASE_URL=http://localhost:11434
#   OLLAMA_MODEL=llama3.2:3b
#
# Option B: Ollama Cloud
#   Free tier: ~250k tokens/hour then paused until the hour resets.
#   Get your API key at: https://ollama.com/settings/keys
#   IMPORTANT: Base URL is https://ollama.com (NOT https://ollama.com/api)
#
#   Recommended models (small, fast, low token usage):
#     gemma3:1b       — fastest, fits Vercel free tier 10s limit
#     gemma3:4b       — better quality, needs Vercel Pro or Railway
#     ministral-3:3b  — lightweight alternative, needs longer timeout

OLLAMA_BASE_URL=https://ollama.com
OLLAMA_MODEL=gemma3:1b
OLLAMA_API_KEY=
