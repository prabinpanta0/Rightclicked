# Google reCAPTCHA v3 site key (optional — leave empty to skip in dev)
VITE_RECAPTCHA_SITE_KEY=

# Backend API URL (leave empty for local dev — Vite proxy handles it)
# Set to your Vercel backend URL in production, e.g.:
# VITE_API_URL=https://rightclicked-backend.vercel.app/api
VITE_API_URL=

# Optional: override extension ZIP download URL shown on /download page
# Example:
# VITE_EXTENSION_ZIP_URL=https://github.com/<owner>/<repo>/releases/download/<tag>/Rightclicked.zip
VITE_EXTENSION_ZIP_URL=
